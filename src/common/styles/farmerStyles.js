import { StyleSheet, StatusBar } from 'react-native';

import COLORS from '../colors/colors';

const FARMER_STYLES = StyleSheet.create({
  container: {
    flex: 1,
  },
  fPortalBtn: {
    height: 1,
    width: 3,
    backgroundColor: 'green',
  },
  Content: {
    marginTop:5,
  }

});

export default FARMER_STYLES;
